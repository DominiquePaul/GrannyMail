TWILIO_ACCOUNT_SID="your-twilio-account-sid"
TWILIO_AUTH_TOKEN="your-twilio-auth-token"
TWILIO_PHONE_NUMBER="the-phone-number-of-your-twilio-account"

FLASK_APP=server.py # name of your flask app
FLASK_ENV=development

DATA_DIR="./memos" # directory or cloud storage location where you want to save your voice memos audio and related data
MODEL_SIZE="small" # the size of the Whisper Model used. Better model = better (but slower) transcription. Options are [tiny, base, small, medium, large].

OPENAI_KEY = "your-openai-api-key"